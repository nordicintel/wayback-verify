Recorded Archive responses used by the unit tests.

- `cdx_w3_iso8859.json`: rows returned by the live CDX API on 2026-09-25 for
  `https://www.w3.org/TR/PNG/iso_8859-1.txt` (trimmed to four rows). Note the
  `http://…:80/` originals the CDX server folds into the same exact match.
- `spn_status_*.json`: the success, pending and error examples from the
  "Save Page Now 2 Public API Docs" (revision 2026-07-22).
